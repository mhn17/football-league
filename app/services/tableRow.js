/**
 * services/tableRow.js
 */

var TableRow = function() {
	var teamId,
		teamName,
		matchesPlayed,
		matchesWon,
		matchesDrawn,
		matchesLost,
		goalsFor,
		goalsAgainst,
		goalDifference,
		points;
};

/**
 * Module exports
 */
module.exports = TableRow;